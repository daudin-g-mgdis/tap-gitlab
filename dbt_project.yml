name: 'tap_gitlab'
version: '0.0.3'
config-version: 2
vars:
  tap_gitlab:
    schema:                  # the schema where the raw gitlab tables are located
    ultimate_license:        # whether tables (e.g. epics) only available on Ultimate Accounts are available

source-paths: ["models"]     # paths with source code to compile
analysis-paths: ["analysis"] # path with analysis files which are compiled, but not run
target-path: "target"        # path for compiled code
clean-targets: ["target"]    # directories removed by the clean task
test-paths: ["test"]         # where to store test results
data-paths: ["data"]         # load CSVs from this directory with `dbt seed`

models:
  tap_gitlab:
    base:
      materialized: table
    transform:
      materialized: table
